/** 对齐 Spring `APP_PIC_FEE`（2000 点）的默认整图翻译扣费。 */
export const DEFAULT_PICTURE_TRANSLATE_TOKEN_COST = 2000;

/** 单次成功文生图的默认定额扣费（未在 TokenBillingRule 配置 baseTokenCost 时使用）。 */
export const DEFAULT_IMAGE_GENERATION_IMAGE_TOKEN_COST = 5000;
