---
description: 前端 UI 展示层改动前置阅读规则
globs: app/routes/page/**/*,app/routes/component/**/*,app/routes/app.tsx,app/routes/app.*.tsx,app/routes/auth.login/**/*
alwaysApply: false
---

# 前端 UI 任务前置规则

当任务涉及**优化、新增或调整前端展示 UI**（布局、组件、样式、页面文案呈现、交互反馈），且修改路径落在 `docs/DESIGN.md` 适用范围内时：

必须先读取 `docs/DESIGN.md`。主路径交互/布局改动先出样例，用户看过后再实现或测试。

## 执行要求

1. 先读 `docs/DESIGN.md`。
2. **先出交互样例再改产品代码**（流程见根 `AGENTS.md` 第 7、12 节）：新增或改主路径交互/布局时，先给可点的 Canvas 样例，用户看过后再改 `app/` / `admin/`。纯文案、修回归、复现已有交互的像素修正可跳过。
3. 对照文档 **Tool Templates** / **Component Guidance**，保持 Ant Design + Tailwind 与共享组件模式一致。
4. 若需求与文档冲突，以用户明确指令为准，并在改动说明中指出偏离点。
5. 保持最小改动：不修改 `app/server/**`、鉴权与 Webhook，除非用户明确要求。
6. 商户可见文案走 i18n；角色名使用 `AI Assistant`。
