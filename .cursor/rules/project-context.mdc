---
description: Spark Agent 入口：按任务规模分档执行仓库入口步骤
alwaysApply: true
---

# Spark Agent 入口规则

根 `AGENTS.md` 是本仓库的入口与代码导航，并且已作为常驻规则随上下文注入——注入的就是磁盘当前版本，不必为「怕拿到旧副本」再完整读一遍。只有在不确定或仅凭历史记忆时才重新读取。

## 按任务规模分档（详见 `AGENTS.md` §0）

- **A 档 · 只读问答与定位**（不产生 diff）：直接查代码回答，不需要 `git status`，不需要写实现方案。硬要求只有一条——不推测没打开过的代码。
- **B 档 · 单点改动**（typo、单文件小修、补测试、调文案）：`git status --short` + 按需读领域文档，然后直接改。
- **C 档 · 跨文件 / 跨域 / 触及数据与外部系统**：走完 `AGENTS.md` §0 六步。其中读文档、`git status`、`rg` 核对调用链彼此无依赖，并行发起而不是串成多轮。

拿不准属于哪一档时按更低一档起步，发现范围变大再补齐步骤。

## 改动约束

- 最小改动；不做未被要求的重构、重命名与清理。
- 保持文案与命名一致，角色名统一使用 `AI Assistant`。
- 未被明确要求时，不改动鉴权、部署流程、密钥相关逻辑。
- C 档的协作节奏见 `.cursor/skills/deliberate-collab/SKILL.md`；领域细则按 `AGENTS.md` 第 6 节路由表按需读取。
- 若 `AGENTS.md` 与旧文档冲突，优先级见 `AGENTS.md` §0。
