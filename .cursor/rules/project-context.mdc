---
description: Spark Agent 入口：AGENTS.md 为地图与硬门禁，默认自主推进
alwaysApply: true
---

# Spark Agent 入口规则

根 `AGENTS.md` 是本仓库的入口与代码导航，并且已作为常驻规则随上下文注入——注入的就是磁盘当前版本，不必为「怕拿到旧副本」再完整读一遍。只有在不确定或仅凭历史记忆时才重新读取。

## 默认节奏

- 查代码 → 直接做 → 按风险自选验证。
- 不推测没打开过的代码。
- 有产品/技术分叉或不可逆操作时再对齐；复杂协作可按需参考 `.cursor/skills/deliberate-collab/SKILL.md`。
- 领域细则见 `AGENTS.md` 第 6 节文档索引（按需），以及按路径触发的领域规则。

## 改动约束

- 最小改动；不做未被要求的重构、重命名与清理。
- 保持文案与命名一致，角色名统一使用 `AI Assistant`。
- 未被明确要求时，不改动鉴权、部署流程、密钥相关逻辑。
- 若 `AGENTS.md` 与旧文档冲突，优先级见 `AGENTS.md` §0。
