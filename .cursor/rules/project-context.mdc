---
description: Spark 项目上下文与改动约束
alwaysApply: true
---

# Spark 项目上下文规则

每次开始修改代码前，请先阅读 `PROJECT_CONTEXT.md`，并遵循其中的约定与边界。

## 执行要求
1. 在动手改代码前，先确认需求影响范围，并阅读对应文件。
2. 尽量做最小改动，避免无关重构。
3. 保持文案与命名一致，角色名统一使用 `AI Assistant`。
4. 未被明确要求时，不改动鉴权、部署流程、密钥相关逻辑。
5. 完成改动后，检查是否引入新的 lint 问题与明显回归。
